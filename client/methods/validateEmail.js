// if (Meteor.isClient) {
// 	Meteor.methods({
// 		validateEmail: function(email) {
// 			if (!email) {
// 				return false;
// 			}
// 			var eVer = /^([\w-]+(?:\.[\w-]+)*)@((?:[\w-]+\.)*\w[\w-]{0,66})\.([a-z]{2,6}(?:\.[a-z]{2})?)$/i;
// 			var isValid = eVer.test(email);

// 			return isValid;
// 		}
// 	});
// }
